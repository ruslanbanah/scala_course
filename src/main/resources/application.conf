app.name = "user-api"

http {
  root = "localhost"
  port = 8080
}

db {
  h2mem {
    profile = "slick.jdbc.H2Profile$"
    db {
      connectionPool = disabled
      driver = "org.h2.Driver"
      url = "jdbc:h2:mem:tsql1;INIT=runscript from 'src/main/resources/db/migration/create_table.sql'"
      user = "ruslanbanah"
      password = ""
      numThreads = 10
    }
  }
  postgre {
    profile = "slick.jdbc.PostgresProfile$"
    db {
      driver = "org.postgresql.Driver"
      databaseName = "sample"
      url = "jdbc:postgresql://localhost/sample"
      user = "ruslanbanah"
      password = ""
      numThreads = 10
    }
  }
}