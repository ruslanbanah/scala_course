app.name = "user-api"

http {
  root = "localhost"
  port = 8080
}

db {
  h2 {
    profile = "slick.jdbc.H2Profile$"
    db {
      connectionPool = disabled
      driver = "org.h2.Driver"
      url = "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;INIT=runscript from 'classpath:db/init.sql'"
    }
  }
  postgre {
    profile = "slick.jdbc.PostgresProfile$"
    db {
      driver = "org.postgresql.Driver"
      databaseName = "sample"
      url = "jdbc:postgresql://localhost/sample"
      user = "ruslanbanah"
      password = ""
      numThreads = 10
    }
  }
}