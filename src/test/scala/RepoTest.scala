import org.scalatest._

class RepoTest extends FlatSpec {
  val userRep = new UserRep()
  val deviceRep = new UserRep()

//  it should "return positive result" in {
//    a.validate shouldEqual Right(a)
//  }

}